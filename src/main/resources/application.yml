server:
  port: 8080
  servlet:
    context-path: /MeiBeautyOfficial

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/meibeautydatabase?characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Taipei
    username: AmyHu
    password: Weare235074weare
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  security:
    user:
      name: amy
      password: 1234

  thymeleaf:
    cache: false
    
  devtools:
    restart:
      enabled: true
    
logging:
  level:
    org:
      hibernate:
        SQL: debug
        type.descriptor.sql.BasicBinder: trace
    